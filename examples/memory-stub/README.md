# Example project memory (for local plugin development)

See [docs/phase-0-memory-api.md](../docs/phase-0-memory-api.md).

```text
.dsh/memory/
├── index.md
├── domains/
│   └── example.md
└── decisions/
```

Copy this tree into a Harness project root to test recall/remember once the plugin exists.
