# Demo project — shared memory playground

English | [中文](README.zh.md)

Minimal git project for the [dual-preset demo walkthrough](../../docs/demo-walkthrough.md).

```bash
cd examples/demo-project
git init    # required for stable project-root resolution
```

Pre-seeded `.dsh/memory/` shows **after Preset A wrote memory** — follow the walkthrough for live `remember` / `recall` with Harness.
