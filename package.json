{
  "name": "open-preset-harness",
  "version": "0.0.0",
  "private": true,
  "description": "DSH Memory plugin monorepo — dsh-tool-project-memory at packages/tool-project-memory (recall, remember, memory_status across presets)",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "doc:check": "node -e \"console.log('Docs + packages monorepo.')\"",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/warrenop/open-preset-harness.git"
  },
  "keywords": [
    "dsh-plugin",
    "deepseek-harness",
    "memory",
    "agent",
    "preset",
    "project-memory",
    "organizational-memory",
    "dsh-tool-project-memory"
  ]
}
